extends ../layouts/common.pug

block content
    h1.text-center CREATE VISITOR

    form(action="/visitor/create", method="POST")
        .form-group 
            label.font-weight-bold Name:
            input.form-control(type="text", name="name", value="")

        .form-group
            label.font-weight-bold Age:
            input.form-control(type="text", name="age", value="")
        button.btn.btn-primary Create